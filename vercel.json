{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" }
  ],
  "buildCommand": "apt-get update && apt-get install -y python3-distutils && pip install -r requirements.txt"
}
